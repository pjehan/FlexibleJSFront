'use strict';

var async = require('async');

var custom = {

  loadData: function(locals, page, lang, database, db, template) {
    var functions = [];

    

    return functions;
  }

}

module.exports = custom;
